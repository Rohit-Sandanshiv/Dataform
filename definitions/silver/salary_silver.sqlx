config {
    type: "table",
    schema : "silver"
}

SELECT 
    LEVEL, 
    COUNT(*) AS EMPLOYEE_COUNT, 
    CAST(ROUND(AVG(SALARY) / 1000) AS STRING) || 'K' AS AVG_SALARY
    FROM ${ref("salary_raw")}
GROUP BY LEVEL